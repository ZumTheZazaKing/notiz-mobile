import { StyleSheet } from 'react-native';

export const settingsStyles = StyleSheet.create({
    container: {
        flex: 1,
        padding:10
    },
    deleteAll: {
        color: 'red',
        fontSize: 16,
    }
})